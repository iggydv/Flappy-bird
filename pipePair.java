

public class pipePair {

    double gap = 0.8;
    double topHeight = Math.floor(Math.random()*(gap-0.4)+0.4);
    double bottomHeight = 1 - topHeight - gap;

    // draw top pipe
    // draw bottom pipe

    public pipePair() {
        
    }
}